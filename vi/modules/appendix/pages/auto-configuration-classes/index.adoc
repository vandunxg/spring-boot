[appendix]
[[appendix.auto-configuration-classes]]
= Auto-configuration Class

Appendix này chứa chi tiết về toàn bộ auto-configuration class do Spring Boot cung cấp, kèm link tới tài liệu và source code.
Hãy nhớ xem thêm conditions report trong application để biết chi tiết hơn về feature nào đang được bật.
(Để làm vậy, khởi động app với `--debug` hoặc `-Ddebug`, hoặc trong application có Actuator thì dùng endpoint `conditions`).
